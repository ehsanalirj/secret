// Utility for real-time multi-user collaboration (Figma-style)
// Placeholder for WebSocket/CRDT logic

export function connectRealtime(projectId, onUpdate) {
  // Connect to real-time backend (WebSocket, CRDT, etc.)
  // onUpdate: callback for remote changes
  // TODO: Implement real-time sync logic
  return {
    sendUpdate: (data) => {},
    disconnect: () => {},
  };
}
